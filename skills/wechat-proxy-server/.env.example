# 微信公众号凭据（必须）
WECHAT_APPID=你的微信AppID
WECHAT_APPSECRET=你的微信AppSecret

# 代理服务鉴权密钥（必须，客户端调用时带 X-API-Key 头）
# 生成方式: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
PROXY_API_KEY=请替换为一个长随机字符串

# 可选：服务端口（默认 8000）
# PORT=8000
